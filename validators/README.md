# Validators

Prototype schema validators, policy checks, quality gates, and evaluation tools
belong here. Validators must link to the experiment they test and state whether
they target a draft idea or a released Codex contract.

A stable validation contract belongs in `codex`; a production validator belongs
in `platform`. Lab implementations may be discarded or rewritten during
promotion.
