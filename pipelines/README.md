# Pipelines

Experimental multi-step creative-production workflows and orchestration patterns
belong here. Record step boundaries, inputs, outputs, failure handling,
observability, reproducibility, and evaluation measures.

Do not connect public experiments to real private content or production secrets.
Proven pipeline implementations are hardened in `platform`; shared contracts are
accepted in `codex` first.
