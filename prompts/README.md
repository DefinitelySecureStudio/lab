# Prompts

Exploratory prompt designs, variants, and evaluations belong here. Group work by
experiment ID and record the model, parameters, tools, evaluation rubric, and
known limitations needed to interpret results.

Stable prompt input/output contracts are promoted to `codex`. Production prompt
instances that include private or unrevealed context do not belong in this public
repository, even when their template is safe.
